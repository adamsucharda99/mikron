import { ImageOptions, ReferenceOptions } from 'sanity';

export const imageOptions: ImageOptions = {
  hotspot: true,
  accept: 'image/jpeg, image/png',
};

export const referenceOptions: ReferenceOptions = {
  disableNew: true,
};
